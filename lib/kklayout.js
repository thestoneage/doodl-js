"use strict";

function kkLayout (graph, dimension, callback) {
	var K = 1.0,
			EPSILON = 0.1,
			LENGTH_FACTOR = 0.9,
			MAX_ITERATIONS = 2000,
			DISCONNECTED_MULTIPLIER = 0.5,
			adjust_for_gravity = true,
			exchange_vertice = true,
			locations = circleLayout(graph, dimensions);
			
			
			
			
}